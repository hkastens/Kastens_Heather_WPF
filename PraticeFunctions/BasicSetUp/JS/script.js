//Functions - Basic Structure

var width = 5; //var scope

function outptMsg(){
	console.log ("Hello World");

}


function calcArea(){
	var width = 20;
	var height = 30;
	var area = width * height;
	console.log(area);
}
calcArea(); //Function execution, can be reused and repeated

consol.log(Width); //will show what is out of funtion since this code is out of funtition as well. 
