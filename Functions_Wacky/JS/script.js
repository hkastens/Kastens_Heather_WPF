//Heather Kastens
//Functions Wacky
//November 21, 2013


var number= prompt ("What number are you wanting to calculate to the third power?");{
	var thirdPower = function (number, number, numer)
	number = Number (number);

	var total = (number * number * number);
	console.log(total);
}
